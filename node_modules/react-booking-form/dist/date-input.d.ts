/// <reference types="react" />
import { BookingForm } from "lib";
declare type DateInputType = {
    placeholder?: string;
    inputComponent?: any;
    className?: string;
    form: BookingForm;
    name: string;
};
export declare const DateInput: ({ placeholder, inputComponent: InputComponent, className, name, form, }: DateInputType) => JSX.Element;
export {};
