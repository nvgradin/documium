/// <reference types="react" />
import { BookingForm } from "lib";
export declare const useLocationData: ({ form, name, debounceDelay, formatResults, }: {
    form: BookingForm;
    name: string;
    debounceDelay: number;
    formatResults: (results: any) => any;
}) => {
    options: import("lib").LocationOption[];
    isLoading: boolean;
    loadOptions: import("react").MutableRefObject<(input: any) => any>;
};
